# function type declaration syntax

```scala
(A, B, ...) => C
```

# function literals syntax

```scala
(parameter: type, ...) => expression
```

```scala
val sayHi = () => "hi!"
val add1 = (x: Int) => x+1
```
