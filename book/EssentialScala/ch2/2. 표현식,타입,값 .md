# 타입, 표현식, 값

```
# 타입
- 컴파일 타임에 존재
- Int, Float 타입 모두 메모리에서 32-bit 로 표현된다

# 표현식
- 컴파일타임에 존재
- 값으로 변환된다
- 타입을 가진다

# 값
- 런 타임에 존재
- 모든 값은 객체다
- 값은 메모리에 존재
- 값은 타입을 가지지않는다
- ( 예시: 2 라는 표현식은 특정한 비트로 평가되어서 메모리에 저장 )

```
