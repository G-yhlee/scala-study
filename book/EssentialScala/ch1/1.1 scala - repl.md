```scala
scala> "Hello world!"
val res0: String = Hello world!

// indetifier => res0
// type => Int
// a value => 6

scala> :type println("Hello world!")
Unit

// Unit 타입은 자바의 void 같은 타입이다

```

```
# 표현식
- 모든 표현식은 type 과 value 를 갖는다
- 타입은 컴파일 타임에 결정
- 값은 런타임에 결정
```
